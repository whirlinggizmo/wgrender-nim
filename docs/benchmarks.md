# wgrender-nim benchmarks

Generated by `tools/benchmarks.py`: do not edit. Baseline measured 2026-09-24 on 13th Gen Intel(R) Core(TM) i9-13900HX, Linux, Brave Browser 153.1.95.104, Emscripten 5.0.7, against wgrender `399ede3`.

`simple` compiled Nim -> C -> one wasm, beside the C. The C row and the call costs are wgrender's baseline (its `bench/results.json`); every binding is collected in wgrender's `docs/benchmarks.md`.

## Download size

Everything a first visit downloads before the first frame, assets aside: the wasm, every JS file and the page. Each file compressed on its own, as a browser fetches it, and summed. `simple` in every configuration, webgl2, no threads, release. wasm, JS and page are raw bytes.

| Configuration | wasm | JS | page | total raw | total gzip | total brotli | vs C (brotli) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| C | 695,714 | 65,959 | 10,672 | 772,345 | 321,684 | 262,550 | 1.00x |
| Nim -> C | 720,009 | 54,980 | 8,647 | 783,636 | 331,204 | 271,231 | 1.03x |

## Frame cost

Chrome's own CPU accounting over 8 s of steady state (`tools/bench/pagebench.py`), the median of three runs: the frame interval is capped at the display rate and hides the work inside it.

| Configuration | script (ms/frame) | task (ms/frame) | script, all runs |
| --- | ---: | ---: | --- |
| C | 0.49 | 0.78 | 0.45, 0.49, 0.52 |
| Nim -> C | 0.52 | 0.80 | 0.50, 0.52, 0.53 |

## JS heap and GC

V8's traced collections over 10 s at 60 fps (`tools/bench/pagebench.py`). Only the JS heap: a collector inside the wasm (hxcpp's) is not visible here at all.

| Configuration | game code runs in | alloc (B/frame) | alloc (MB/min) | collections traced | late frames |
| --- | --- | ---: | ---: | --- | ---: |
| C | wasm | 771 | 2.6 | none | 0 |
| Nim -> C | wasm | 1,456 | 5.0 | none | 0 |

Code running in the wasm allocates nothing on the JS heap itself, so those rows (771 to 1,456 B/frame here) are the page's own noise: Emscripten's glue, the page and the measuring. Their order means nothing.

## Stress

Game logic at scale (`tools/bench/stress.c`, ported line for line): N entities moved every frame, one wgr call each, about N/240 of them replaced a frame (objects destroyed and created; in Nim, Beef and Haxe, allocated and freed or collected), and 49 lines of formatted text. On the real GPU (Xvfb, ANGLE on Vulkan): headless Chrome rasterizes this many sprites in software, slowly enough to bound the frame.

Script time per frame, the median of three runs:

| Configuration | n = 1,000 | n = 5,000 | n = 10,000 |
| --- | ---: | ---: | ---: |
| C | 0.43 | 1.40 | 2.66 |
| Nim -> C | 0.46 | 1.48 | 2.85 |

With 8 ms of other work burned in every frame, so a pause has little slack to hide in: JS heap allocation, the collections V8 traced, and frames over 20 ms (`tools/bench/pagebench.py`, 10 s).

| Configuration | n | alloc (MB/min) | collections traced | worst frame (ms) | late frames |
| --- | ---: | ---: | --- | ---: | ---: |
| C | 1,000 | 1.4 | 1 minor, 0.4 ms | 16.67 | 0 |
| C | 5,000 | 1.4 | 1 minor, 0.4 ms | 16.67 | 0 |
| C | 10,000 | 1.4 | 1 minor, 0.5 ms | 16.67 | 0 |
| Nim -> C | 1,000 | 1.7 | 1 minor, 0.5 ms | 16.67 | 0 |
| Nim -> C | 5,000 | 1.7 | 1 minor, 0.4 ms | 16.67 | 0 |
| Nim -> C | 10,000 | 1.7 | 1 minor, 0.3 ms | 16.67 | 0 |

## Calls from a JS guest

What a call from JS into wgrender's wasm costs, against the same call made inside the wasm (`tools/bench/callbench`, Chrome/153.0.8010.53, median of 7 runs of 5,000,000 calls). The JS side marshals as a JS guest binding does: struct results read into a new object, strings copied in with stringToUTF8.

| Shape | like | JS -> wasm (ns) | inside wasm (ns) | boundary (ns) | calls per ms of JS |
| --- | --- | ---: | ---: | ---: | ---: |
| tint | `wgr_model_set_tint` | 3.25 | 0.45 | 2.80 | 308,000 |
| transform | `wgr_model_set_transform` | 5.82 | 0.45 | 5.37 | 172,000 |
| struct | `wgr_input_get_mouse_state` | 12.63 | 1.43 | 11.21 | 79,000 |
| string | `wgr_text_measure` | 26.61 | 2.49 | 24.12 | 38,000 |

## Sources

| Project | measured | against wgrender | toolchains |
| --- | --- | --- | --- |
| wgrender-c | 2026-09-24 | `399ede3` (self) | Emscripten 5.0.7 |
| wgrender-nim | 2026-09-24 | `399ede3` (sibling checkout) | Nim 2.2.12 |
